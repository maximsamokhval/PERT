[project]
name = "backend"
version = "0.1.0"
description = "PERT Estimation SDLC Tool"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mypy>=1.20.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
